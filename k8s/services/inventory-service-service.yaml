apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: medilink
  labels:
    app: inventory-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8006
    targetPort: 8006
    protocol: TCP
    name: http

  selector:
    app: inventory-service
