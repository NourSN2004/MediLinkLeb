apiVersion: v1
kind: Service
metadata:
  name: doctor-service
  namespace: medilink
  labels:
    app: doctor-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http

  selector:
    app: doctor-service
