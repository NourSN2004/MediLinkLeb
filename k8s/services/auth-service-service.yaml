apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: medilink
  labels:
    app: auth-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http

  selector:
    app: auth-service
