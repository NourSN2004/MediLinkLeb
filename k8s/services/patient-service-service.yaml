apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: medilink
  labels:
    app: patient-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
    name: http

  selector:
    app: patient-service
